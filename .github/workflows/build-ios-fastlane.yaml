name: build-ios-fastlane

on:
  workflow_dispatch:

env:
  FLUTTER_VERSION: '3.44.4'

jobs:
  build-ios:
    runs-on: macos-latest
    steps:
      - name: Checkout
        uses: actions/checkout@v4
        with:
          submodules: recursive
          token: ${{ secrets.BOARD_SDK_PAT || secrets.GITHUB_TOKEN }}

      - name: Setup Go
        uses: actions/setup-go@v5
        with:
          go-version: '1.26.4'
          cache-dependency-path: |
            core/go.sum

      - name: Setup Flutter
        uses: subosito/flutter-action@v2
        with:
          channel: master
          flutter-version: ${{ env.FLUTTER_VERSION }}
          cache: true

      - name: Setup Xcode
        run: |
          sudo xcode-select -s /Applications/Xcode_26.3.app || sudo xcode-select -s /Applications/Xcode_26.2.app
          xcodebuild -version

      - name: Get Flutter Dependency
        run: |
          flutter --version
          flutter pub get

      - name: Setup Code Signing
        run: |
          # 创建专用钥匙串
          security create-keychain -p temp build.keychain
          security default-keychain -s build.keychain
          security unlock-keychain -p temp build.keychain

          # 导入 iPhone Distribution 证书
          echo "${{ secrets.IOS_CERTIFICATE }}" | base64 --decode > cert.p12
          security import cert.p12 -k build.keychain \
            -P "${{ secrets.IOS_CERTIFICATE_PASSWORD }}" -T /usr/bin/codesign
          security set-key-partition-list \
            -S apple-tool:,apple:,codesign: -s -k temp build.keychain
          security delete-certificate -c "Apple Distribution: Faying Cheng" build.keychain 2>/dev/null || true

          # 安装仓库中的 Provisioning Profiles（无需 secrets）
          mkdir -p ~/Library/MobileDevice/Provisioning\ Profiles
          mkdir -p ~/Library/Developer/Xcode/UserData/Provisioning\ Profiles
          mkdir -p ~/private_keys

          cp ios/ToWorld.mobileprovision \
            ~/Library/MobileDevice/Provisioning\ Profiles/340b6121-77d2-4d0a-81f5-e9241d1524b5.mobileprovision
          cp ios/ToWorld.mobileprovision \
            ~/Library/Developer/Xcode/UserData/Provisioning\ Profiles/340b6121-77d2-4d0a-81f5-e9241d1524b5.mobileprovision
          cp ios/ToWorldPacketTunnel.mobileprovision \
            ~/Library/MobileDevice/Provisioning\ Profiles/efbd09e0-c128-4857-b7ee-987f9b4260e4.mobileprovision
          cp ios/ToWorldPacketTunnel.mobileprovision \
            ~/Library/Developer/Xcode/UserData/Provisioning\ Profiles/efbd09e0-c128-4857-b7ee-987f9b4260e4.mobileprovision
          echo "[signing] profiles installed from repo ($(ls ~/Library/MobileDevice/Provisioning\ Profiles/ | wc -l) files)"

          # App Store Connect API Key
          echo "${{ secrets.ASC_API_KEY }}" > ~/private_keys/AuthKey_${{ secrets.ASC_KEY_ID }}.p8

      - name: Build & Export IPA
        run: |
          make core-ios
          flutter precache --ios

          cd ios && pod install && cd ..
          test -d ios/Runner.xcworkspace || { echo "ERROR: pod install 未生成 Runner.xcworkspace"; exit 1; }

          # Archive（不签名）
          echo "[xcodebuild] archiving without signing"
          xcodebuild -workspace ios/Runner.xcworkspace \
            -scheme Runner \
            -destination 'generic/platform=iOS' \
            -configuration Release \
            -archivePath build/ios/Runner.xcarchive \
            IPHONEOS_DEPLOYMENT_TARGET=15.0 \
            CODE_SIGNING_ALLOWED=NO \
            archive

          # Export（Manual 签名 + ToWorld profiles）
          echo "[xcodebuild] exporting with manual signing"
          cat > ExportOptions.plist <<PLIST
          <?xml version="1.0" encoding="UTF-8"?>
          <!DOCTYPE plist PUBLIC "-//Apple//DTD PLIST 1.0//EN" "http://www.apple.com/DTDs/PropertyList-1.0.dtd">
          <plist version="1.0">
          <dict>
              <key>method</key>
              <string>app-store-connect</string>
              <key>teamID</key>
              <string>9ZKNBU3VL4</string>
              <key>signingCertificate</key>
              <string>iOS Distribution</string>
              <key>signingStyle</key>
              <string>manual</string>
              <key>provisioningProfiles</key>
              <dict>
                  <key>uk.toworld.flclash</key>
                  <string>ToWorld</string>
                  <key>uk.toworld.flclash.PacketTunnel</key>
                  <string>ToWorldPacketTunnel</string>
              </dict>
          </dict>
          </plist>
          PLIST

          xcodebuild -exportArchive \
            -archivePath build/ios/Runner.xcarchive \
            -exportPath dist/ \
            -exportOptionsPlist ExportOptions.plist

          # 重命名 IPA
          mv dist/Runner.ipa dist/FlClash-ios-arm64.ipa 2>/dev/null || \
          mv dist/fl_clash.ipa dist/FlClash-ios-arm64.ipa 2>/dev/null || \
          { ls -la dist/; echo "ERROR: 找不到 IPA"; exit 1; }

      - name: Upload to TestFlight
        run: |
          xcrun altool --upload-app -f dist/FlClash-ios-arm64.ipa \
            --apiKey ${{ secrets.ASC_KEY_ID }} \
            --apiIssuer ${{ secrets.ASC_ISSUER_ID }} \
            --type ios

      - name: Upload artifact
        uses: actions/upload-artifact@v4
        with:
          name: FlClash-ios-fastlane
          path: ./dist/FlClash-ios-arm64.ipa
          overwrite: true
